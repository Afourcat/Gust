image: "alpine"
before_script:
  - apt-get update -qq && apt-get install -y -qq rust cargo
  - bundle install

stages:
  - build
  - test
  - deploy

job1:
  stage: build
  script: "cargo build"

job2:
  stage: test
  script: "cargo test"

after_script:
  - cargo clean
